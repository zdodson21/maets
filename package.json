{
  "name": "maets",
  "version": "1.0.0",
  "description": "A clone website of Steam for an ETI 461 project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:build": "web-dev-server --root-dir dist --app-index index.html --open",
    "start": "web-dev-server"
  },
  "author": "Zach Dodson",
  "license": "ISC",
  "dependencies": {
    "lit": "^3.1.2"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.3",
    "http-server": "^14.1.1"
  }
}
